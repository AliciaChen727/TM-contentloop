{
  "name": "contentloop-ops-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Telegram ChatOps bot — dispatch ContentLoop's coding/bug-fix agent (Phase 3C). Runs on Railway, not Vercel.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node index.mjs",
    "dev": "node index.mjs"
  }
}
